## 0.1.0

Initial release.

- **feat**: Add file scanner for `lib/` directory with exclude patterns
- **feat**: Add import parser to resolve internal dependencies
- **feat**: Add graph builder with Node, Edge, Graph models
- **feat**: Add JSON output format (`graph.json`)
- **feat**: Add Mermaid output format (`graph.md`)
- **feat**: Add HTML output format (`graph.html`)
- **feat**: Add feature-based grouping with subgraphs
- **feat**: Add `fdg.yaml` configuration file support
- **feat**: Add CLI options (`-f`, `-o`, `-e`, `-v`, `-h`)
- **docs**: Add README with usage examples
- **docs**: Add API documentation comments
- **test**: Add unit tests for models, config, and mermaid generator
