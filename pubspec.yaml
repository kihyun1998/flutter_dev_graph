name: flutter_dev_graph
description: A CLI tool to visualize Flutter/Dart project dependencies as a graph with feature-based grouping support.
version: 0.1.0
repository: https://github.com/mj-studio-library/flutter_dev_graph
homepage: https://github.com/mj-studio-library/flutter_dev_graph
topics:
  - dependency
  - graph
  - visualization
  - flutter
  - cli

environment:
  sdk: ">=3.0.0 <4.0.0"

executables:
  fdg: fdg

dependencies:
  args: ^2.7.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
